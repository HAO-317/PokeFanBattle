{
  "name": "pokefanbattle",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "vite build && node fix-paths.js",
    "dev": "vite",
    "preview": "vite preview",
    "deploy": "vite build && node fix-paths.js && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "typescript": "~5.7.2",
    "vite": "^6.2.0"
  }
}
